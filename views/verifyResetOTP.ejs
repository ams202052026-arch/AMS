<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify OTP - AMS</title>
    <link rel="stylesheet" href="/css/scrollbar.css">
    <link rel="stylesheet" href="/css/auth.css">
</head>
<body>
    <div class="container">
        <div class="form-wrapper">
            <div class="form-header">
                <div class="logo-container">
                    <a href="/"><img src="/image/logo.png" alt="AMS Logo" class="logo"></a>
                </div>
                <h1 class="form-title">Verify OTP</h1>
                <p class="form-subtitle">Enter the 5-digit code sent to your email</p>
                <p class="hint">Code expires in 60 seconds</p>
            </div>

            <% if (error) { %>
                <div class="alert alert-error"><%= error %></div>
            <% } %>

            <form action="/forgot-password/verify-otp" method="POST" class="login-form">
                <div class="form-group">
                    <label for="code" class="form-label">OTP Code</label>
                    <div class="input-wrapper">
                        <input 
                            type="text" 
                            id="code"
                            name="code" 
                            class="form-input"
                            placeholder="12345" 
                            maxlength="5" 
                            pattern="[0-9]{5}" 
                            required 
                            autofocus
                            style="text-align: center; font-size: 1.5rem; letter-spacing: 0.5rem;"
                        >
                    </div>
                </div>

                <button type="submit" class="submit-btn">
                    <span>Verify Code</span>
                    <svg class="btn-icon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <polyline points="20 6 9 17 4 12"/>
                    </svg>
                </button>

                <div class="form-footer">
                    <a href="/forgot-password" class="signup-link">← Request New Code</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
