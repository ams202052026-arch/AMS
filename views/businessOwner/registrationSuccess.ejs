<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Application Submitted</title>
    <link rel="icon" type="image/png" href="/image/favicon.png">
    <link rel="stylesheet" href="/css/scrollbar.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #ffffff;
            color: #1a1a1a;
            line-height: 1.6;
            padding: 20px;
        }

        .container {
            max-width: 500px;
            margin: 0 auto;
        }

        .back-link {
            margin-bottom: 40px;
        }

        .back-link a {
            color: #666;
            text-decoration: none;
            font-size: 14px;
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .back-link a:hover {
            color: #1a1a1a;
        }

        .content {
            text-align: center;
        }

        .status-icon {
            width: 64px;
            height: 64px;
            background: #1a1a1a;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 24px;
        }

        .checkmark {
            width: 32px;
            height: 32px;
            border: 3px solid #ffffff;
            border-radius: 50%;
            position: relative;
        }

        .checkmark::after {
            content: '';
            position: absolute;
            left: 8px;
            top: 4px;
            width: 10px;
            height: 16px;
            border: solid #ffffff;
            border-width: 0 3px 3px 0;
            transform: rotate(45deg);
        }

        h1 {
            font-size: 28px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .business-name {
            font-size: 17px;
            font-weight: 500;
            color: #666;
            margin-bottom: 32px;
        }

        .info-section {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 16px;
            text-align: left;
        }

        .info-section h3 {
            font-size: 15px;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 12px;
        }

        .info-section p {
            font-size: 14px;
            color: #666;
            margin-bottom: 8px;
            line-height: 1.5;
        }

        .info-section p:last-child {
            margin-bottom: 0;
        }

        .timeline {
            list-style: none;
            padding: 0;
        }

        .timeline li {
            font-size: 14px;
            color: #666;
            padding-left: 24px;
            position: relative;
            margin-bottom: 10px;
            line-height: 1.5;
        }

        .timeline li:last-child {
            margin-bottom: 0;
        }

        .timeline li::before {
            content: '';
            position: absolute;
            left: 0;
            top: 7px;
            width: 6px;
            height: 6px;
            background: #1a1a1a;
            border-radius: 50%;
        }

        .button-group {
            margin-top: 32px;
        }

        .primary-btn {
            display: inline-block;
            padding: 14px 32px;
            background: #1a1a1a;
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 15px;
            font-weight: 600;
            transition: background 0.2s;
        }

        .primary-btn:hover {
            background: #333;
        }

        /* Mobile */
        @media (max-width: 768px) {
            body {
                padding: 12px;
            }

            .back-link {
                margin-bottom: 24px;
            }

            .back-link a {
                font-size: 13px;
            }

            h1 {
                font-size: 20px;
            }

            .business-name {
                font-size: 15px;
                margin-bottom: 24px;
            }

            .status-icon {
                width: 48px;
                height: 48px;
                margin-bottom: 20px;
            }

            .checkmark {
                width: 24px;
                height: 24px;
                border-width: 2px;
            }

            .checkmark::after {
                left: 6px;
                top: 3px;
                width: 7px;
                height: 12px;
                border-width: 0 2px 2px 0;
            }

            .info-section {
                padding: 14px;
                margin-bottom: 12px;
            }

            .info-section h3 {
                font-size: 13px;
                margin-bottom: 10px;
            }

            .info-section p,
            .timeline li {
                font-size: 12px;
                margin-bottom: 6px;
            }

            .timeline li {
                padding-left: 18px;
                margin-bottom: 8px;
            }

            .timeline li::before {
                width: 5px;
                height: 5px;
                top: 6px;
            }

            .button-group {
                margin-top: 24px;
            }

            .primary-btn {
                padding: 12px 24px;
                font-size: 14px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="back-link">
            <% if (isUnifiedMode) { %>
                <a href="/home">← Back</a>
            <% } else { %>
                <a href="/">← Back</a>
            <% } %>
        </div>

        <div class="content">
            <div class="status-icon">
                <div class="checkmark"></div>
            </div>
            
            <h1>Application Submitted</h1>
            <div class="business-name"><%= businessName %></div>

            <div class="info-section">
                <h3>Review Process</h3>
                <ul class="timeline">
                    <li>Admin reviews application</li>
                    <li>Documents verified</li>
                    <li>Email notification sent</li>
                    <li>Approval in 1-3 business days</li>
                </ul>
            </div>

            <div class="info-section">
                <h3>What's Next</h3>
                <p>Check your email for updates on verification status.</p>
                <p>Once approved, you can login and start adding services.</p>
            </div>

            <div class="button-group">
                <% if (isUnifiedMode) { %>
                    <a href="/home" class="primary-btn">Continue</a>
                <% } else if (typeof redirectUrl !== 'undefined' && redirectUrl) { %>
                    <a href="<%= redirectUrl %>" class="primary-btn">Done</a>
                <% } else { %>
                    <a href="/" class="primary-btn">Done</a>
                <% } %>
            </div>
        </div>
    </div>
</body>
</html>
