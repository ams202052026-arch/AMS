<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= business.businessName %> - Services</title>
    <link rel="stylesheet" href="/css/businessOwner/main.css">
</head>
<body>
    <div class="dashboard-layout">
        <!-- Shared Sidebar -->
        <%- include('../partials/sidebar', { currentPage: 'services' }) %>

        <!-- Main Content -->
        <main class="main-content">
            <div class="services-header">
                <h1>Services Management</h1>
                <a href="/business-owner/services/add" class="btn-primary">+ Add Service</a>
            </div>

            <% if (services.length > 0) { %>
                <div class="services-grid">
                    <% services.forEach(service => { %>
                        <div class="service-card">
                            <img src="<%= service.image %>" alt="<%= service.name %>" class="service-image">
                            <div class="service-content">
                                <h3 class="service-name"><%= service.name %></h3>
                                <p class="service-description"><%= service.description %></p>
                                
                                <div class="service-details">
                                    <span class="service-price">₱<%= service.price.toLocaleString() %></span>
                                    <span class="service-duration"><%= service.duration %> mins</span>
                                </div>

                                <div class="service-meta">
                                    <span class="service-category">
                                        <% 
                                        const validCategories = ['hair', 'skin', 'nails', 'spa', 'other'];
                                        const displayCategory = validCategories.includes(service.category) 
                                            ? service.category.charAt(0).toUpperCase() + service.category.slice(1)
                                            : 'Other';
                                        %>
                                        <%= displayCategory %>
                                    </span>
                                    <span class="service-points">+<%= service.pointsEarned %> pts</span>
                                </div>

                                <div class="service-meta">
                                    <span class="status-badge <%= service.isActive ? 'active' : 'inactive' %>">
                                        <%= service.isActive ? 'Active' : 'Inactive' %>
                                    </span>
                                </div>

                                <div class="service-actions">
                                    <a href="/business-owner/services/<%= service._id %>/edit" class="btn-sm btn-secondary">Edit</a>
                                    <% if (service.isActive) { %>
                                        <form action="/business-owner/services/<%= service._id %>/deactivate" method="POST" style="display: inline;">
                                            <button type="submit" class="btn-sm btn-danger">Deactivate</button>
                                        </form>
                                    <% } else { %>
                                        <form action="/business-owner/services/<%= service._id %>/activate" method="POST" style="display: inline;">
                                            <button type="submit" class="btn-sm btn-secondary">Activate</button>
                                        </form>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <div class="icon">💼</div>
                    <h3>No Services Yet</h3>
                    <p>Start building your service menu by adding your first service.</p>
                    <a href="/business-owner/services/add" class="btn-primary">+ Add Your First Service</a>
                </div>
            <% } %>
        </main>
    </div>
</body>
</html>