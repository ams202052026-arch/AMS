<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Application Status</title>
    <link rel="icon" type="image/png" href="/image/favicon.png">
    <link rel="stylesheet" href="/css/scrollbar.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: #ffffff;
            color: #1a1a1a;
            line-height: 1.6;
            padding: 20px;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
        }

        .back-link {
            margin-bottom: 40px;
        }

        .back-link a {
            color: #666;
            text-decoration: none;
            font-size: 14px;
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .back-link a:hover {
            color: #1a1a1a;
        }

        .page-header {
            margin-bottom: 30px;
            text-align: center;
        }

        .page-header h1 {
            font-size: 28px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .page-header p {
            color: #666;
            font-size: 15px;
        }

        .error-message {
            background: #f8f9fa;
            color: #1a1a1a;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            font-size: 14px;
            border: 1px solid #e0e0e0;
        }

        .status-card {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 24px;
            margin-bottom: 20px;
            text-align: center;
        }

        .status-card h3 {
            font-size: 18px;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 12px;
        }

        .status-card p {
            font-size: 14px;
            color: #666;
            margin-bottom: 12px;
        }

        .business-info {
            background: #ffffff;
            padding: 16px;
            border-radius: 8px;
            margin: 16px 0;
            text-align: left;
            border: 1px solid #e0e0e0;
        }

        .business-info h4 {
            font-size: 14px;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 10px;
        }

        .business-info p {
            font-size: 13px;
            color: #666;
            margin: 6px 0;
        }

        .business-info strong {
            color: #1a1a1a;
        }

        .rejection-reason {
            background: #ffffff;
            padding: 16px;
            border-radius: 8px;
            margin: 16px 0;
            text-align: left;
            border: 1px solid #e0e0e0;
        }

        .rejection-reason h4 {
            font-size: 14px;
            font-weight: 600;
            color: #1a1a1a;
            margin-bottom: 8px;
        }

        .rejection-reason p {
            font-size: 13px;
            color: #666;
        }

        .status-note {
            font-size: 13px;
            color: #999;
            margin-top: 12px;
        }

        .btn {
            display: inline-block;
            padding: 14px 32px;
            background: #1a1a1a;
            color: #ffffff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 15px;
            font-weight: 600;
            transition: background 0.2s;
            margin-top: 16px;
        }

        .btn:hover {
            background: #333;
        }

        /* Mobile */
        @media (max-width: 768px) {
            body {
                padding: 12px;
            }

            .back-link {
                margin-bottom: 24px;
            }

            .back-link a {
                font-size: 13px;
            }

            .page-header {
                margin-bottom: 20px;
            }

            .page-header h1 {
                font-size: 20px;
            }

            .page-header p {
                font-size: 13px;
            }

            .error-message {
                padding: 10px;
                font-size: 12px;
                margin-bottom: 16px;
            }

            .status-card {
                padding: 16px;
                margin-bottom: 16px;
            }

            .status-card h3 {
                font-size: 15px;
                margin-bottom: 10px;
            }

            .status-card p {
                font-size: 12px;
                margin-bottom: 10px;
            }

            .business-info,
            .rejection-reason {
                padding: 12px;
                margin: 12px 0;
            }

            .business-info h4,
            .rejection-reason h4 {
                font-size: 13px;
                margin-bottom: 8px;
            }

            .business-info p,
            .rejection-reason p {
                font-size: 12px;
                margin: 4px 0;
            }

            .status-note {
                font-size: 11px;
                margin-top: 10px;
            }

            .btn {
                padding: 12px 24px;
                font-size: 14px;
                margin-top: 12px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="back-link">
            <a href="/home">← Back</a>
        </div>

        <div class="page-header">
            <h1>Application Status</h1>
            <p>Track your business verification</p>
        </div>

        <% if (error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>

        <% if (applicationStatus === 'not_applied') { %>
            <div class="status-card">
                <h3>No Application</h3>
                <p>You haven't submitted a business application yet.</p>
                <a href="/business/register" class="btn">Apply Now</a>
            </div>
        <% } else if (applicationStatus === 'pending') { %>
            <div class="status-card">
                <h3>Under Review</h3>
                <p>Your application is being reviewed by our team.</p>
                <div class="business-info">
                    <h4>Business Details</h4>
                    <p><strong>Name:</strong> <%= business.businessName %></p>
                    <p><strong>Type:</strong> <%= business.businessType %></p>
                    <p><strong>Phone:</strong> <%= business.phoneNumber %></p>
                    <p><strong>Submitted:</strong> <%= business.createdAt.toLocaleDateString() %></p>
                </div>
                <p class="status-note">We'll notify you once review is complete</p>
            </div>
        <% } else if (applicationStatus === 'approved') { %>
            <div class="status-card">
                <h3>Approved</h3>
                <p>Your business application has been approved.</p>
                <div class="business-info">
                    <h4>Business Details</h4>
                    <p><strong>Name:</strong> <%= business.businessName %></p>
                    <p><strong>Type:</strong> <%= business.businessType %></p>
                    <p><strong>Phone:</strong> <%= business.phoneNumber %></p>
                    <p><strong>Approved:</strong> <%= business.approvedAt ? business.approvedAt.toLocaleDateString() : 'Recently' %></p>
                </div>
                <a href="/switch-to-business" class="btn">Go to Business</a>
            </div>
        <% } else if (applicationStatus === 'rejected') { %>
            <div class="status-card">
                <h3>Rejected</h3>
                <p>Your application was not approved.</p>
                <% if (business.rejectionReason) { %>
                    <div class="rejection-reason">
                        <h4>Reason</h4>
                        <p><%= business.rejectionReason %></p>
                    </div>
                <% } %>
                <div class="business-info">
                    <h4>Original Application</h4>
                    <p><strong>Name:</strong> <%= business.businessName %></p>
                    <p><strong>Type:</strong> <%= business.businessType %></p>
                    <p><strong>Submitted:</strong> <%= business.createdAt.toLocaleDateString() %></p>
                </div>
                <a href="/business/reapply" class="btn">Reapply</a>
            </div>
        <% } else { %>
            <div class="status-card">
                <h3>Error</h3>
                <p>Couldn't load your application status.</p>
                <a href="/home" class="btn">Back to Home</a>
            </div>
        <% } %>
    </div>
</body>
</html>
